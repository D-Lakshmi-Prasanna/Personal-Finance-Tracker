package com.example.finance.util;

/*
 * author: Prasanna
 * 
 * Role is an enumeration representing different roles that can be assigned to users within the system.
 * It has two values: USER and ADMIN, which define the type of permissions and access levels a user can have.
 */
public enum Role {
    // Represents a regular user with standard access privileges
	 USER,
	// Represents an admin user with elevated permissions and access to management functions
    ADMIN 
}
